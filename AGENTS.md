# 建造者规约

给在这个仓库里工作的人和 agent。

1. **`docs/DESIGN.md` 是唯一产品定义。** 方向变了先改它再改代码；每个结论要能回答「为什么不是另一个」。不留 changelog，历史在 git。
2. **从零开始，不搬代码。** 不从任何其它项目复制代码、配置或文档；借鉴的是想法，用自己的话重写。依赖只加公开发布的库。
3. **真机才算做完。** 「已支持」「已验证」必须有 `docs/spikes/` 里一条带日期的记录；spikes 只增不改。
4. **不虚报。** 做不到的地方在 CLI 输出、界面和文档里明说；失败形态是设计出来的，不是遇到再说。
5. **准入判据。** 新增的名词、页面、命令参数、依赖，必须让 DESIGN §1.2 的某一个时刻更短或更可信，否则不加。
6. **玩家侧零门槛是硬约束。** 任何改动若要求玩家登录、安装、或多一次跳转，先改 DESIGN。
7. **一个产品，一个主域。** 发现、关注、账号、发布与管理统一在 `playtest.run`；作品仍在 `<slug>.playtest.run` 独立源运行。根域不得执行作者脚本，平台会话只用 host-only Cookie，写操作校验精确 Origin，隧道剥离平台凭据。暂不迁独立运行域，不将同站子域误称为完整安全隔离。
8. **文案。** 对外只有英文、不做 i18n：CLI 输出与帮助、玩家页、邮件与推送、控制台、安装脚本、README。对内保持中文：设计文档、代码注释、spike、提交信息。英文用第一次使用的人看得懂的词，不用内部比喻，不用营销口吻。
9. **提交信息**一句话说清为什么，中文。不提交密钥；`.env*` 永不入库。
