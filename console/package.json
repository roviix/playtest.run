{
  "name": "playtest-console",
  "version": "0.1.0",
  "private": true,
  "description": "playtest 控制台：作品时间线、会话点名册、反馈流",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.28.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.14"
  }
}
